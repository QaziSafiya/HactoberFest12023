#include <iostream>

using namespace std;

int main()
{
    string str = "testsample";
    int count[26] = {0};
    for (int i = 0; i < str.length(); i++)
    {
        count[str[i] - 'a']++;
    }
    int max = 0, ans = 0;
    for (int i = 0; i < 26; i++)
    {
        if (max < count[i])
        {
            ans = i;
            max = count[i];
        }
    }
    char finalans = 'a' + ans;
    cout << finalans;

    return 0;
}
